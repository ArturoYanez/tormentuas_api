
import React from "react";

const TemplatesPanel = ({
  templateData, setTemplateData,
  selectedTemplateId, setSelectedTemplateId,
  editTemplate, setEditTemplate,
  editingTemplate, setEditingTemplate,
  showCreateTemplate, setShowCreateTemplate,
  newTemplate, setNewTemplate,
  handleEditTemplate, handleTemplateChange,
  handleSaveTemplate, handleDeleteTemplate, handleCreateTemplate,
  toast
}) => (
  <>
    {/* Copia aquí la UI de plantillas de respuestas (cards, edición, creación...) */}
  </>
);
export default TemplatesPanel;
